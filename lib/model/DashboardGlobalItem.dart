import 'package:flutter/material.dart';

class DashboardGlobalItem {
  String title;
  int value;
  String date;
  IconData icon;
  Color color;

  DashboardGlobalItem(
      {this.title, this.value, this.date, this.icon, this.color});
}
